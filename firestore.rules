rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    match /users/{userId} {
      // السماح للجميع بقراءة الملف الشخصي لعرض الأسماء تحت الفيديوهات لضمان ظهور المحتوى للجميع
      allow read: if true;
      allow create, update: if isOwner(userId);
      allow delete: if isOwner(userId);
    }

    match /videos/{videoId} {
      // الفيديوهات عامة للقراءة للجميع لضمان ظهورها في الخلاصة لجميع المستخدمين
      allow read: if true;
      allow create: if isSignedIn();
      allow update, delete: if isSignedIn() && resource.data.uploaderId == request.auth.uid;
    }
  }
}